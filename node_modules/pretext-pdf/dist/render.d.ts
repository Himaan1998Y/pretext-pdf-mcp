import { PDFDocument } from '@cantoo/pdf-lib';
import type { PdfDocument, PaginatedDocument, FontMap, ImageMap, PageGeometry } from './types.js';
/**
 * Stage 5: Render.
 * Takes the paginated document + pre-initialized pdfDoc (with fonts already embedded)
 * and produces the final PDF bytes.
 *
 * pdfDoc is NOT created here — it comes from index.ts with fonts already embedded.
 * imageMap contains pre-embedded PDFImage instances.
 */
export declare function renderDocument(paginatedDoc: PaginatedDocument, doc: PdfDocument, fontMap: FontMap, imageMap: ImageMap, pdfDoc: PDFDocument, geo: PageGeometry): Promise<Uint8Array>;
//# sourceMappingURL=render.d.ts.map