name: pretext-pdf
version: 1.5.10
description: "PDF generation for AI agents. Create GST invoices, reports, tables, forms, and markdown-to-PDF from structured JSON — no headless browser, pure Node.js."
startCommand:
  type: stdio
  command: npx
  args: ["-y", "pretext-pdf-mcp"]
configSchema:
  type: object
  properties: {}
exampleConfig: {}
